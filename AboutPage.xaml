<Page
    x:Class="Tagmgr.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tagmgr"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel
            Padding="32"
            Spacing="16"
            MaxWidth="640"
            HorizontalAlignment="Left">

            <StackPanel Orientation="Horizontal" Spacing="16">
                <FontIcon
                    Glyph="&#xE8EC;"
                    FontSize="36"
                    Foreground="#0F6CBD"/>
                <TextBlock
                    Text="Tagmgr"
                    FontSize="32"
                    FontWeight="Bold"
                    VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock
                Text="Build 0.0.3(20260919+3) by Costellion"
                Foreground="Gray"/>

            <TextBlock
                Text="一个轻量的文件标签管理工具，用于给本地文件打标签并按标签快速筛选。"
                TextWrapping="Wrap"/>

            <TextBlock
                Text="主要功能"
                FontSize="18"
                FontWeight="SemiBold"
                Margin="0,8,0,0"/>

            <StackPanel Spacing="6">
                <TextBlock Text="• 给任意本地文件添加多个标签"/>
                <TextBlock Text="• 支持多选文件，一键批量添加/移除标签"/>
                <TextBlock Text="• 按多个标签交集筛选文件"/>
                <TextBlock Text="• 标签数据保存在本地，不上传任何信息"/>
            </StackPanel>

            <TextBlock
                Text="技术说明"
                FontSize="18"
                FontWeight="SemiBold"
                Margin="0,8,0,0"/>

            <StackPanel Spacing="6">
                <TextBlock Text="• WinUI 3 (Windows App SDK)"/>
                <TextBlock Text="• .NET / C#"/>
                <TextBlock Text="• System.Text.Json 本地存储"/>
            </StackPanel>

            <TextBlock
                Text="数据保存位置：%LOCALAPPDATA%\Tagmgr\tags.json"
                Foreground="Gray"
                FontSize="12"
                Margin="0,16,0,0"
                TextWrapping="Wrap"/>

        </StackPanel>
    </ScrollViewer>
</Page>